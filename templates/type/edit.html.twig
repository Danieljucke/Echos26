{% extends 'base_2.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
{#    <h1>Edit Type</h1>#}

{#    {{ include('type/_form.html.twig', {'button_label': 'Update'}) }}#}

{#    <a href="{{ path('app_type_index') }}">back to list</a>#}

{#    {{ include('type/_delete_form.html.twig') }}#}
    <div class="needs-validation">
        {{ form_start(form) }}
        <div class="card">
            <div class="card-header">
                <h4>Editer le type</h4>
            </div>
            <div class="card-body">
                <div class="form-group">
                    <label>Libele</label>
                    {{ form_widget(form.libelle) }}
                    <div class="invalid-feedback">
                        Please fill in the title
                    </div>
                </div>
                <div class="form-group">
                    <label>statut</label>
                    {{ form_widget(form.statut) }}
                </div>
            </div>
            <div class="card-footer">
                {#            {{ form_widget(form.Enregistrer, {'attr':{'class':'btn btn-primary'} }) }}#}
                <button class="btn btn-primary">{{ button_label|default('Update') }}</button>
            </div>
        </div>
        {{ form_end(form) }}
        <div class="float-right">
            <a href="{{ path('app_type_index') }}" class="btn btn-primary">back to list</a>
        </div>
    </div>
{% endblock %}
